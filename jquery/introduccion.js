$(document).ready(function(){
    
    //$(selector).accion();
    
    /*
        $ - Indica que vamos a acceder a jquery.
        selector - accedemos al elemento que vamos a trabajar.
        accion - lo que se quiere hacer con el elemento seleccionado.
    */

    $('h1').hide();
});

//-----------

$(function(){

});