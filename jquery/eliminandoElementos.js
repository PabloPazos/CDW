$(document).ready(function() {

    //Eliminar elemento como sus hijos.
    // $('#contenedor').remove();

    //Eliminar elementos hijos.
    // $('#contenedor').children().remove();
    // $('#contenedor').children('#segunda').remove();
    $('#contenedor').empty();
});